version: 1
frontend:
  preBuild:
    commands:
      - nvm use 20.18.1
      - npm install
  build:
    commands:
      - yarn run build

  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
