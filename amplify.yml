version: 1
frontend:
  preBuild:
    commands:
      - nvm use 20.18.1
      - npm install
  build:
    commands:
      - npm run build
  artifacts:
    baseDirectory: next-ts/.next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - .npm/**/*
      - node_modules/**/*
